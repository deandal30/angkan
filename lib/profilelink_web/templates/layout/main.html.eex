<!DOCTYPE html>
<html lang="en">

<head>
  <script>
    if (self == top) {
      document.documentElement.style.display = 'block';
    } else {
      top.location = self.location;
    }
  </script>

  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=1">
  <meta name="description" content="">
  <meta name="author" content="">

  <link rel="shortcut icon" href="<%= static_path(@conn, "/favicon.ico") %>" type="image/x-icon">
  <link rel="icon" href="<%= static_path(@conn, "/favicon.ico") %>" type="image/x-icon">

  <title>ANGKAN</title>
  <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  <link rel="stylesheet" href="<%= static_path(@conn, "/css/components.css") %>">

</head>

<body id="auth">
  <div class="auth-wrapper">
    <div class="slide-content">
      <a href="/">
        <img class="ui medium image brand-logo" src="<%= static_path(@conn, "/images/test.png") %>">
      </a>
      <div class="content">
        <h1 class="header">
          Find your Family.
          </br>
          Discover Yourself.
        </h1>
        <p class="p-details">
          Bring to life your family’s history by exploring the lives of those that came before you.
        </p>
      </div>

      <div class="side-content-footer">
        <p class="p-footer">Made by:</p>
        <a href="" target="_">
        </a>
        <span class="sp-footer"> ANGKAN @ Version 0.0.1</span>
      </div>
    </div>
    <div class="main-panel">
      <%= render @view_module, @view_template, assigns %>
    </div>
  </div>
  <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
</body>

</html>

<%
  info = get_flash(@conn, :info)
  error = get_flash(@conn, :error)
  warning = get_flash(@conn, :warning)
%>
<%= if info do %>
  <script type="text/javascript">
    $(function () {
      alertify.success('<i class="close icon"></i><%= info %>')
      });
  </script>
<% end %>

<%= if error do %>
  <script type="text/javascript">
    $(function () {
      alertify.error('<i class="close icon"></i><%= error %>')
    });
  </script>
<% end %>

<%= if warning do %>
  <script type="text/javascript">
    $(function () {
      alertify.warning('<i class="close icon"></i><%= warning %>')
    });
  </script>
<% end %>